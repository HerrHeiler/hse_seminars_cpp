#include <iostream>
#include <string>

int main() {
    std::string i, initiall;
    int s = 0;
    std::cin >> i;
    initiall = i;
    if (i[0] == '-') i.erase(0, 1);
    for (int j = i.length() - 1; j >= 0; --j) {
        
    }
    return 0;
}