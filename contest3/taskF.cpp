#include <iostream>

int main() {
    std::string word, answer = "", discarded = "aehiouwy";
    std::cin >> word;
    answer += word[0];
    for (int i = 0; i < word.length(); ++i) {
        
    }
    return 0;
}